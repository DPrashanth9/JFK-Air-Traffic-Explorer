# FastAPI and server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Neo4j driver
neo4j>=5.15.0

# Environment variables
python-dotenv>=1.0.0

# Data validation (using latest versions with pre-built wheels)
pydantic>=2.9.0
pydantic-settings>=2.5.0

# CORS is built into FastAPI, no separate package needed
